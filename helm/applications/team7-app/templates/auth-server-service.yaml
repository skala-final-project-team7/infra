apiVersion: v1
kind: Service
metadata:
  name: auth-server
  namespace: {{ .Values.global.namespace }}
  labels:
    app: auth-server
spec:
  selector:
    app: auth-server
  ports:
    - name: http
      port: {{ .Values.services.authServer.port }}
      targetPort: http
      protocol: TCP
