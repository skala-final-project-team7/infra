apiVersion: v1
kind: Service
metadata:
  name: ingestion-api
  namespace: {{ .Values.global.namespace }}
  labels:
    app: ingestion-api
spec:
  selector:
    app: ingestion-api
  ports:
    - name: http
      port: {{ .Values.services.ingestionApi.port }}
      targetPort: http
      protocol: TCP
